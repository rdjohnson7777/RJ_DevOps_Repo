---
- name: Bootstrap Python on new RHEL instances that utilize ansible
  hosts: rhel8_hosts
  gather_facts: false
  become: yes
  tasks:
    - name: Install Python 3 on RHEL
      raw: dnf install -y python39
